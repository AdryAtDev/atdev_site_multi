{
	"name": "eleventy-sample",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "eleventy",
		"start": "eleventy --serve --port=9090",
		"dev": "clear && rm -rf _site && eleventy --serve --watch --port=9090"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@11ty/eleventy": "^2.0.0",
		"clean-css": "^5.3.2",
		"html-minifier": "^4.0.0",
		"terser": "^5.18.2"
	},
	"devDependencies": {
		"netlify-plugin-cache": "^1.0.3"
	}
}
